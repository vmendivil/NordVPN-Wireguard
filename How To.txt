# Extract WireGuard connection data
#     https://forum.opnsense.org/index.php?topic=21350.0
# Whitelist subnet to keep connection to ssh after connecting to NordVpn
#     https://www.reddit.com/r/nordvpn/comments/oqykxp/nordvpn_connect_cuts_off_raspberry_pi_from_network/

Run v1

# Dedicated IP
#     Use NordVpnDedicatedIpToWG.sh (NOT v1/v2 — the recommendations API does not
#     return dedicated-IP servers). It reads peer key + endpoint from the live
#     tunnel (wg show nordlynx peers / endpoints) instead of the API.
#       ./NordVpnDedicatedIpToWG.sh            # connects via --group dedicated_ip
#       ./NordVpnDedicatedIpToWG.sh us4955     # pin your exact assigned server (preferred)
#     Endpoint = negotiated wg endpoint (double-NAT means it may differ from the
#     public dedicated exit IP, which is written as a comment in the .conf).

